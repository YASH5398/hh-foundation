import React from 'react';

// ChatBadge component - deprecated, use direct buttons instead
// This is kept for backward compatibility but should not be used
const ChatBadge = () => {
  return null;
};

export default ChatBadge;